<?xml version="1.0"?>
<!--
/**
 * GrupoAwamotos LayoutFix
 *
 * Fix 1: notification.messages broken reference
 * Clears the module-level "before=user" ordering from notification.messages
 * so the theme can safely move both blocks to header.inner.right without
 * a "Broken reference" reorder warning.
 *
 * Fix 2: mst.feature-request broken reference
 * Mirasvit Core tries to add mst.feature-request to page.main.container
 * which does not exist in adminhtml. Remove the block to suppress the warning.
 */
-->
<page xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="urn:magento:framework:View/Layout/etc/page_configuration.xsd">
    <body>
        <!-- Fix 1: notification.messages broken reference
             theme-adminhtml-backend does:
               <move element="notification.messages" after="user" destination="header.inner.right"/>
             'user' is in 'header', 'notification.messages' ends up in 'header.inner.right'
             → cross-container ordering = "Broken reference" on every admin page load.
             We re-move the block within the same destination (header.inner.right) with
             after="-" to override the broken after="user" constraint. -->
        <move element="notification.messages" destination="header.inner.right" after="-"/>

        <!-- Fix 2: remove Mirasvit feature-request widget (page.main.container does not exist in adminhtml) -->
        <referenceBlock name="mst.feature-request" remove="true"/>
    </body>
</page>
