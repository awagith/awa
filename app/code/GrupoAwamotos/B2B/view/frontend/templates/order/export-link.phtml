<?php
/**
 * CSV Export Link Template
 * P1-2: CSV Export for B2B customers
 *
 * @var \GrupoAwamotos\B2B\Block\Order\ExportLink $block
 */

declare(strict_types=1);

if (!$block->canExport()) {
    return;
}
?>
<div class="b2b-export-actions">
    <a href="<?= $block->escapeUrl($block->getExportUrl()) ?>"
       class="b2b-export-csv-btn action primary"
       title="<?= $block->escapeHtmlAttr($block->getTooltip()) ?>"
       aria-label="<?= $block->escapeHtmlAttr($block->getLinkLabel()) ?>">
        <span class="b2b-export-icon" aria-hidden="true">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                <polyline points="7 10 12 15 17 10"/>
                <line x1="12" y1="15" x2="12" y2="3"/>
            </svg>
        </span>
        <span><?= $block->escapeHtml($block->getLinkLabel()) ?></span>
    </a>
</div>

<style>
    .b2b-export-actions {
        margin-bottom: 20px;
        display: flex;
        justify-content: flex-end;
    }

    .b2b-export-csv-btn {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 10px 18px;
        background: var(--awa-green, #15803d);
        color: #fff;
        border-radius: var(--awa-radius-sm, 4px);
        text-decoration: none;
        font-size: 14px;
        font-weight: 600;
        transition: background 0.2s ease, transform 0.1s ease;
    }

    .b2b-export-csv-btn:hover {
        background: var(--awa-green-dark, #166534);
        color: #fff;
        transform: translateY(-1px);
    }

    .b2b-export-csv-btn:active {
        transform: translateY(0);
    }

    .b2b-export-icon {
        display: flex;
        align-items: center;
    }

    @media (max-width: 480px) {
        .b2b-export-actions {
            justify-content: center;
        }

        .b2b-export-csv-btn {
            width: 100%;
            justify-content: center;
        }
    }
</style>
