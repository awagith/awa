<?php
/**
 * Quote Request Form Template
 *
 * @var \GrupoAwamotos\B2B\Block\Quote\Form $block
 */
?>
<div class="b2b-quote-form-container b2b-page-shell">

    <div class="page-title-wrapper">
        <h1 class="page-title">
            <span class="base"><?= $block->escapeHtml(__('Solicitar Cotação')) ?></span>
        </h1>
    </div>

    <?php if (!$block->hasItems()): ?>

        <!-- ── Carrinho vazio ────────────────────────────────────────────── -->
        <div class="alert alert-warning">
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <?= $block->escapeHtml(__('Seu carrinho está vazio. Adicione produtos antes de solicitar uma cotação.')) ?>
        </div>
        <a href="<?= $block->escapeUrl($block->getUrl('/')) ?>" class="btn btn-primary">
            <span class="glyphicon glyphicon-arrow-left"></span>
            <?= $block->escapeHtml(__('Continuar Comprando')) ?>
        </a>

    <?php else: ?>

        <!-- ── Layout de duas colunas ────────────────────────────────────── -->
        <div class="row">

            <!-- Coluna esquerda: itens para cotação ─────────────────────── -->
            <div class="col-md-7">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title">
                            <?= $block->escapeHtml(__('Itens para Cotação')) ?>
                        </h3>
                    </div>
                    <div class="table-responsive">
                        <table class="table table-striped table-hover" id="quote-items-table">
                            <thead>
                                <tr>
                                    <th><?= $block->escapeHtml(__('Produto')) ?></th>
                                    <th><?= $block->escapeHtml(__('Qtd')) ?></th>
                                    <th><?= $block->escapeHtml(__('Preço Unit.')) ?></th>
                                    <th><?= $block->escapeHtml(__('Subtotal')) ?></th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php foreach ($block->getCartItems() as $item): ?>
                                    <tr>
                                        <td data-th="<?= $block->escapeHtmlAttr(__('Produto')) ?>">
                                            <strong><?= $block->escapeHtml($item->getName()) ?></strong>
                                            <br><small>SKU: <?= $block->escapeHtml($item->getSku()) ?></small>
                                        </td>
                                        <td data-th="<?= $block->escapeHtmlAttr(__('Qtd')) ?>">
                                            <?= (int) $item->getQty() ?>
                                        </td>
                                        <td data-th="<?= $block->escapeHtmlAttr(__('Preço Unit.')) ?>">
                                            R$ <?= number_format($item->getPrice(), 2, ',', '.') ?>
                                        </td>
                                        <td data-th="<?= $block->escapeHtmlAttr(__('Subtotal')) ?>">
                                            R$ <?= number_format($item->getRowTotal(), 2, ',', '.') ?>
                                        </td>
                                    </tr>
                                <?php endforeach; ?>
                            </tbody>
                            <tfoot>
                                <tr class="b2b-quote-tfoot">
                                    <td colspan="3">
                                        <strong><?= $block->escapeHtml(__('Total Estimado')) ?></strong>
                                    </td>
                                    <td>
                                        <strong>R$ <?= number_format($block->getCartTotal(), 2, ',', '.') ?></strong>
                                    </td>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    <div class="panel-footer">
                        <p class="text-muted small" style="margin:0;">
                            <?= $block->escapeHtml(__('* Os preços são estimados. O valor final será informado na cotação.')) ?>
                        </p>
                    </div>
                </div>
            </div><!-- /.col-md-7 -->

            <!-- Coluna direita: dados do solicitante ────────────────────── -->
            <div class="col-md-5">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title"><?= $block->escapeHtml(__('Seus Dados')) ?></h3>
                    </div>
                    <div class="panel-body">
                        <form action="<?= $block->escapeUrl($block->getSubmitUrl()) ?>"
                              method="post"
                              id="quote-request-form"
                              class="form"
                              data-mage-init='{"validation":{}}'>
                            <?= /* @noEscape */ $block->getBlockHtml('formkey') ?>

                            <div class="form-group">
                                <label class="control-label" for="name">
                                    <?= $block->escapeHtml(__('Nome Completo')) ?>
                                    <span class="required" aria-hidden="true">*</span>
                                </label>
                                <input type="text"
                                       name="name"
                                       id="name"
                                       value="<?= $block->escapeHtmlAttr($block->getCustomerName()) ?>"
                                       class="form-control"
                                       data-validate="{required:true}"
                                       <?= $block->isLoggedIn() ? 'readonly' : '' ?>>
                            </div>

                            <div class="form-group">
                                <label class="control-label" for="email">
                                    <?= $block->escapeHtml(__('E-mail')) ?>
                                    <span class="required" aria-hidden="true">*</span>
                                </label>
                                <input type="email"
                                       name="email"
                                       id="email"
                                       value="<?= $block->escapeHtmlAttr($block->getCustomerEmail()) ?>"
                                       class="form-control"
                                       data-validate="{required:true, 'validate-email':true}"
                                       <?= $block->isLoggedIn() ? 'readonly' : '' ?>>
                            </div>

                            <div class="form-group">
                                <label class="control-label" for="company_name">
                                    <?= $block->escapeHtml(__('Razão Social / Empresa')) ?>
                                </label>
                                <input type="text"
                                       name="company_name"
                                       id="company_name"
                                       value="<?= $block->escapeHtmlAttr($block->getCompanyName()) ?>"
                                       class="form-control">
                            </div>

                            <div class="form-group">
                                <label class="control-label" for="cnpj">
                                    <?= $block->escapeHtml(__('CNPJ')) ?>
                                </label>
                                <input type="text"
                                       name="cnpj"
                                       id="cnpj"
                                       value="<?= $block->escapeHtmlAttr($block->getCnpj()) ?>"
                                       class="form-control"
                                       placeholder="00.000.000/0000-00">
                            </div>

                            <div class="form-group">
                                <label class="control-label" for="phone">
                                    <?= $block->escapeHtml(__('Telefone')) ?>
                                </label>
                                <input type="tel"
                                       name="phone"
                                       id="phone"
                                       value="<?= $block->escapeHtmlAttr($block->getPhone()) ?>"
                                       class="form-control"
                                       placeholder="(00) 00000-0000">
                            </div>

                            <div class="form-group">
                                <label class="control-label" for="message">
                                    <?= $block->escapeHtml(__('Mensagem / Observações')) ?>
                                </label>
                                <textarea name="message"
                                          id="message"
                                          class="form-control"
                                          rows="4"
                                          placeholder="<?= $block->escapeHtmlAttr(__('Informe detalhes adicionais, condições especiais desejadas, prazo de entrega, etc.')) ?>"></textarea>
                            </div>

                            <div class="alert alert-info" style="margin-bottom:16px;">
                                <span class="glyphicon glyphicon-info-sign"></span>
                                <?= $block->escapeHtml(__('Sua cotação terá validade de %1 dias após o envio.', $block->getExpiryDays())) ?>
                            </div>

                            <button type="submit"
                                    class="btn btn-primary btn-block btn-lg"
                                    title="<?= $block->escapeHtmlAttr(__('Solicitar Cotação')) ?>">
                                <span class="glyphicon glyphicon-send"></span>
                                <?= $block->escapeHtml(__('Solicitar Cotação')) ?>
                            </button>

                            <div class="text-center" style="margin-top:10px;">
                                <a href="<?= $block->escapeUrl($block->getCartUrl()) ?>"
                                   class="btn btn-link btn-sm">
                                    <?= $block->escapeHtml(__('Voltar ao Carrinho')) ?>
                                </a>
                            </div>

                        </form>
                    </div><!-- /.panel-body -->
                </div><!-- /.panel -->
            </div><!-- /.col-md-5 -->

        </div><!-- /.row -->

    <?php endif; ?>
</div>
