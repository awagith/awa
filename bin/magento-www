#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "$ROOT_DIR"
if [[ "${EUID:-$(id -u)}" -eq 0 ]]; then
    exec runuser -u www-data -- php bin/magento "$@"
fi

exec php bin/magento "$@"
